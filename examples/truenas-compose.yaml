name: aria2

services:
  aria2:
    image: ghcr.io/cmroche/aria2:latest
    pull_policy: always
    restart: unless-stopped
    read_only: true
    cap_drop:
      - ALL
    cap_add:
      - SETUID
      - SETGID
    security_opt:
      - no-new-privileges:true
    environment:
      RPC_SECRET: ${RPC_SECRET:?Set RPC_SECRET before deploying}
      PUID: ${PUID:-568}
      GUID: ${GUID:-568}
      UMASK: ${UMASK:-0022}
      RPC_PORT: "6800"
      DOWNLOAD_DIR: /downloads
      ARIA2_LOG_LEVEL: ${ARIA2_LOG_LEVEL:-notice}
      TZ: ${TZ:-Etc/UTC}
    ports:
      - "${HOST_RPC_PORT:-6800}:6800/tcp"
    volumes:
      - type: bind
        source: ${DOWNLOADS_PATH:?Set DOWNLOADS_PATH before deploying}
        target: /downloads
        read_only: false
    tmpfs:
      - /tmp:mode=1777,size=64m
